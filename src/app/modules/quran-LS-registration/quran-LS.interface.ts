export type TQuranLSUser = {
  userName: string;
  userEmail: string;
  userGender: 'male' | 'female';
  dateOfBirth: string;
  profession: string;
  address: string;
  phoneNumber: string;
  whatsAppNumber: string;
  batchNo: 'quran-LS-2';
};

// LS = learning session
